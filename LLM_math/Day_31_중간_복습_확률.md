# Day 31: 중간 복습 - 확률 (1시간)

## 📖 Day 28-30 복습

### 확률 기초
```
P(A): 0 ~ 1
P(A|B) = P(A∩B) / P(B)
독립: P(A∩B) = P(A)P(B)
```

### 베이즈 정리
```
P(A|B) = P(B|A) × P(A) / P(B)
```

### 정규분포
```
N(μ, σ²)
평균 μ, 분산 σ²
```

---

## 💻 종합 실습

```python
import numpy as np

# 동전 던지기
flips = np.random.choice(['H', 'T'], 1000)
p_h = np.sum(flips == 'H') / len(flips)
print(f"P(앞면) = {p_h:.4f}")

# 정규분포
samples = np.random.normal(0, 1, 1000)
print(f"평균: {np.mean(samples):.4f}")
print(f"분산: {np.var(samples):.4f}")
```

---

## 🎓 다음: 엔트로피!

**불확실성을 어떻게 측정할까?**

---

**복습 완료!**
