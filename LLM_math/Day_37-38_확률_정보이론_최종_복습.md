# Day 37-38: 확률과 정보이론 최종 복습 (2시간)

## 📚 학습 목표
- Day 28-36의 모든 개념 총정리
- LLM과의 연결 완전히 이해하기
- 다음 단계 (LLM 핵심 수학) 준비

---

## 🎯 강의 주제
**"확률에서 정보이론으로, 그리고 LLM으로"**

---

## 📖 전체 복습

### 1. 확률 (Day 28-31)
```
P(A): 0 ~ 1
P(A|B) = P(A∩B) / P(B)
베이즈: P(A|B) = P(B|A)P(A) / P(B)
정규분포: N(μ, σ²)
```

### 2. 엔트로피 (Day 32-33)
```
H(X) = -Σ P(x) log P(x)
Cross Entropy: H(P, Q) = -Σ P log Q
Perplexity = 2^H
```

### 3. 정보 측정 (Day 34-36)
```
상호정보량: I(X;Y) = H(X) - H(X|Y)
정보 이득: IG = H(전) - H(후)
KL Divergence: D(P||Q) = Σ P log(P/Q)
```

---

## 💻 최종 종합 프로젝트

```python
import numpy as np

class LLM_Math_Probability:
    """확률과 정보이론 통합"""

    def __init__(self):
        self.vocab = ['the', 'a', 'cat', 'dog', '.']
        self.vocab_size = len(self.vocab)

    def simulate_language_model(self):
        """간단한 언어 모델 시뮬레이션"""
        print("=== LLM 시뮬레이션 ===\n")

        # Context: "the"
        # 실제 다음 토큰: "cat"
        true_idx = 2

        # 모델 예측 (확률 분포)
        pred_probs = np.array([0.1, 0.15, 0.5, 0.2, 0.05])

        print(f"Context: '{self.vocab[0]}'")
        print(f"실제 다음 토큰: '{self.vocab[true_idx]}'\n")

        print("모델 예측 확률:")
        for i, (word, prob) in enumerate(zip(self.vocab, pred_probs)):
            mark = " ← 정답" if i == true_idx else ""
            print(f"  {word}: {prob:.2f}{mark}")

        # 엔트로피 (모델의 불확실성)
        h = -np.sum(pred_probs * np.log2(pred_probs))
        print(f"\n모델 엔트로피: {h:.4f} bits")

        # Cross Entropy Loss
        true_dist = np.zeros(self.vocab_size)
        true_dist[true_idx] = 1.0
        ce = -np.sum(true_dist * np.log(pred_probs + 1e-10))
        print(f"Cross Entropy Loss: {ce:.4f}")

        # Perplexity
        ppl = np.exp(ce)
        print(f"Perplexity: {ppl:.4f}")
        print(f"  → 모델이 평균 {ppl:.1f}개 선택지 중 고민\n")

        return ce, ppl

    def bayesian_update(self):
        """베이즈 업데이트 시뮬레이션"""
        print("=== 베이즈 업데이트 ===\n")

        # 사전 확률: 토큰 빈도
        prior = np.array([0.3, 0.2, 0.15, 0.25, 0.1])

        # 관찰: context "the" 다음
        # 가능도 P(context|token)
        likelihood = np.array([0.1, 0.2, 0.5, 0.15, 0.05])

        # 사후 확률 (정규화)
        posterior = prior * likelihood
        posterior /= posterior.sum()

        print("사전 확률 (빈도):")
        for word, prob in zip(self.vocab, prior):
            print(f"  {word}: {prob:.2f}")

        print("\n사후 확률 (context 고려):")
        for word, prob in zip(self.vocab, posterior):
            print(f"  {word}: {prob:.2f}")

        print("\n→ 'cat'의 확률이 증가!")

# 실행
model = LLM_Math_Probability()
ce, ppl = model.simulate_language_model()
model.bayesian_update()
```

---

## 📊 개념 맵

```
확률 기초
    ↓
베이즈 정리 → LLM 토큰 예측
    ↓
확률분포 → 가중치 초기화
    ↓
엔트로피 → 불확실성 측정
    ↓
Cross Entropy → 손실 함수
    ↓
Perplexity → 모델 평가
    ↓
정보 이득 → BPE 토크나이저
    ↓
LLM 학습!
```

---

## ✅ 최종 체크포인트

- [ ] **확률의 기본 개념을 이해했나요?**
- [ ] **베이즈 정리를 LLM에 연결할 수 있나요?**
- [ ] **엔트로피의 의미를 설명할 수 있나요?**
- [ ] **Cross Entropy 손실을 이해했나요?**
- [ ] **Perplexity로 모델을 평가할 수 있나요?**
- [ ] **정보 이득과 BPE의 관계를 아나요?**

---

## 🎓 지금까지의 여정

### 배운 것들
1. **기초** (Day 1-10): 수, 함수, 벡터
2. **선형대수** (Day 11-20): 내적, 행렬, PCA
3. **미적분** (Day 21-27): 미분, 연쇄법칙, 경사하강법
4. **확률** (Day 28-38): 확률, 엔트로피, 정보이론

### 다음 단계
**Day 39-48: LLM 핵심 수학**
- BPE 토크나이저
- Attention 메커니즘
- Transformer 아키텍처
- 최종 통합!

---

**수고하셨습니다!** 🎉

**이제 LLM의 핵심으로 들어갑니다!**
