# Day 35: 정보 이득 (Information Gain) (2시간)

## 📚 학습 목표
- 정보 이득의 정의 이해하기
- 의사결정 트리에서의 활용 파악하기
- **BPE와의 연결 이해하기** ⭐

---

## 🎯 강의 주제
**"최선의 결정 내리기"**

---

## 📖 핵심 개념

### 1. 정보 이득 (Information Gain)

**정의**:
```
IG(D, A) = H(D) - Σ (|D_v|/|D|) × H(D_v)

D: 데이터
A: 속성
D_v: 속성 A=v인 데이터
```

**의미**:
```
속성 A로 분할하면 불확실성이 얼마나 줄어드는가?
```

---

### 2. 의사결정 트리

**알고리즘**:
```
1. 정보 이득이 가장 큰 속성 선택
2. 그 속성으로 분할
3. 재귀적으로 반복
```

---

### 3. BPE에서의 정보 이득

**토큰화**:
```
자주 나오는 바이트 쌍 병합
→ 정보 이득 최대화!

압축률 향상 = 엔트로피 감소
```

---

## 💻 Python 실습

```python
import numpy as np

def entropy(labels):
    """엔트로피 계산"""
    unique, counts = np.unique(labels, return_counts=True)
    probs = counts / len(labels)
    return -np.sum(probs * np.log2(probs + 1e-10))

def information_gain(labels, feature):
    """정보 이득"""
    h_before = entropy(labels)

    # 속성별 분할
    unique_vals = np.unique(feature)
    h_after = 0

    for val in unique_vals:
        subset = labels[feature == val]
        weight = len(subset) / len(labels)
        h_after += weight * entropy(subset)

    return h_before - h_after

# 예: 날씨 → 놀이 여부
weather = np.array(['맑음', '맑음', '흐림', '비', '비'])
play = np.array([1, 1, 1, 0, 0])

print("=== 정보 이득 ===\n")
print(f"데이터: {len(play)}개")
print(f"초기 엔트로피: {entropy(play):.4f}\n")

# 날씨로 분할
ig = information_gain(play, weather)
print(f"정보 이득 (날씨): {ig:.4f}")
print("→ 날씨를 알면 불확실성이 줄어듦!")
```

---

## 🔗 LLM 연결점

### BPE 토크나이저
```
"aaabdaaabac" → "ZbdZbac"  (aa → Z)

정보 이득이 큰 쌍을 우선 병합!
```

---

## 🎓 핵심 요약

**정보 이득**: 분할로 인한 불확실성 감소
- 의사결정 트리
- 토크나이저 (BPE)

### 다음 학습
- **Day 36**: 정보이론 종합

---

**최선의 선택은 정보 이득으로!**
